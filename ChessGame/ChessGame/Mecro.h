#pragma once
#include<vector>
#include<Windows.h>
using namespace std;


enum PIECEBLOCK
{
	PIECEBLOCK_PAWN,
	PIECEBLOCK_KNIGHT,
	PIECEBLOCK_BISHOP,
	PIECEBLOCK_ROOK,
	PIECEBLOCK_QUEEN,
	PIECEBLOCK_KING,
};	  

enum BOARDBLOCK
{
	BOARDBLOCK_START,
	BOARDBLOCK_LIGHT=0,
	BOARDBLOCK_DARK,
	BOARDBLOCK_END = 2
};

enum COLOR
{
	COLOR_B,
	COLOR_W,
	COLOR_END = 2
 };

enum PIECENUM
{
	PIECENUM_NONE = 0,
	PIECENUM_PAWN = 8,
	PIECENUM_KNIGHT = 2,
	PIECENUM_BISHOP = 2,
	PIECENUM_ROOK = 2,
	PIECENUM_QUEEN =1,
	PIECENUM_KING = 1,
	PIECENUM_ALL = 16
};

enum IMG
{
	IMG_START,
	IMG_BRDBLOCK = 3,
	IMG_BPIECE = 6,
	IMG_WPIECE = 6,
	IMG_MAX = 15,
	IMG_END
};


#define BLOCK_WIDTH 125
#define BLOCK_HEIGHT 125